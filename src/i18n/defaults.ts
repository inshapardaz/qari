import type { TranslationStrings } from './types';
import { en } from './locales/en';

/** English is the canonical, always-complete translation set. */
export const DEFAULT_TRANSLATIONS: TranslationStrings = en;
